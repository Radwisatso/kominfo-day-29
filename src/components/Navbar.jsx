export default function Navbar() {
  return (
    <header>
      <nav
        style={{
          display: "flex",
          justifyContent: "center",
          gap: "10px"
        }}
      >
        <a href="">Home</a>
        <a href="">About</a>
        <a href="">Contact</a>
      </nav>
    </header>
  );
}
